version: '3.8'
services:
  db:
    image: postgres:15
    container_name: lanchonete_db
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: snoopy2025
      POSTGRES_DB: snoopy_db
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: